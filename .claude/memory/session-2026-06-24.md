<!-- Mirrors the ~/.claude project memory file `session-2026-06-24.md` so it travels with the repo; any Claude instance can read it here. Keep both copies in sync. -->

---
name: session-2026-06-24
description: "2026-06-24 session — SEO overhaul + archive runtime extraction landed; memory corrections; still-open items"
metadata:
  type: project
---

Session on 2026-06-24. Three commits on `master` (NOT pushed): `44d2602`, `54fd604`, `dd4702b`.

## Landed

- **SEO overhaul** (`44d2602`) — was previously sitting uncommitted; finished, build-verified, committed. `src/layouts/Main.astro` now derives per-page canonical/OG URL from the route, emits a JSON-LD `@graph` (Person / ProfessionalService / WebSite), Twitter Card tags, and `title`/`description` defaults; the obsolete `keywords` meta was dropped. Every page passes a unique title+description. Added `@astrojs/sitemap`, `public/llms.txt`, and `docs/seo.md`.
- **Archive runtime extraction** (`54fd604`) — moved the ~390-line client `<script>` (MixItUp wiring, `onMixEnd`, active-filter chips, button availability, open/close, PhotoSwipe lightbox) out of `ArchiveGallery_BoxFilters.astro` into new `src/components/archive/filters.ts`, exposed as `initArchiveFilters()`. Component dropped 1684 → 1301 lines. Behaviour-preserving (reads from DOM as before); markup, `define:vars` CSS, and the fragile mobile/iOS animation layer untouched. `docs/archive-filters.md` updated.
- **Memory corrections** — fixed stale claims: Serena `conventions` said "use `@components`/`@assets` aliases" (wrong; relative-only since 2026-06-11) and overstated tooling (no jsx-a11y plugin, no `.prettierrc`); the "`links` collection unused" claim (in Serena `core` and `cleanup-2026-06-11.md`) was wrong — it IS consumed by the `personal/` variant via `InformationPersonal.astro`.

## Verified

`astro check` 0/0/0 and `npm run build` (exit 0, sitemap + robots generated) after each change.

## Still open (do not re-discover as new)

- **High-res lightbox** — `TODO` at `src/components/gallery/GalleryElement.astro:100` (PhotoSwipe `highResData` commented out). User confirmed 2026-06-24 this is **deliberately not to be done yet** — leave it.
- **Obsolete stash** `stash@{0}` ("WIP on master") — based on a 2025-06-06 commit, references the pre-reorg path `src/components/ArchiveGallery_BoxFilters.astro` and a conflicting eslint/prettier setup. Cannot apply cleanly. Recommend `git stash drop`; the drop was permission-denied this session.
- **`.git` bloat** — 386 MB vs 22 MB working-tree assets (binary/AVIF-frame history). Candidate for git-lfs / history rewrite, or serving large media via Cloudinary.
- **Forked `*Personal` components** — `Navigation*`/`*Personal` + `Information*` are near-duplicates; unification still deferred (visual-regression risk, per `cleanup-2026-06-11.md`).
- **Cosmetic, deferred:** `--dekstop` CSS-var typos (23 uses); OG image filename still has the "Scientfic" typo.
- 3 commits unpushed on `master`.
