---
name: cleanup-2026-06-11
description: "June 2026 repo cleanup — what was removed, and which findings were deliberately deferred"
metadata: 
  node_type: memory
  type: project
  originSessionId: 3e0e20cf-0eb7-4b3e-9c84-31e1e7d3dbd6
---

On 2026-06-11 a legibility/cleanup pass landed in 7 commits (c46ee7a..2979c7b): removed 14 unused npm deps, 8 orphaned components, unused assets, Yarn PnP leftovers, unused path aliases, ~200 unused public font files; extracted shared `src/components/gallery/photoswipeVideo.ts`; added CLAUDE.md and `check`/`lint`/`format` scripts.

Deliberately deferred (don't re-flag as new findings):
- CSS variable typos `--font-size-dekstop` / `--margin-left-dekstop` — used consistently, rename deferred as wide-diff cosmetic.
- Four near-duplicate navigation bars (`NavigationBar*`/`*Personal`) — unification declined due to visual-regression risk.
- `links` content collection is defined in `content.config.ts` with YAML data; the `Information.astro` consumer was dead code and removed. NOTE (corrected 2026-06-24): it is NOT fully unused — the `personal/` variant still consumes it via `InformationPersonal.astro` → `personal/info.astro`.
- OG image filename still contains the "Scientfic" typo (meta description strings were fixed).
