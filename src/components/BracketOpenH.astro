---
interface Props {
 strokewidth?: string;
 strokecolor?: string;
}

const { strokewidth = "1px", strokecolor = "black" } = Astro.props;
---

<div class="svg-container">
    <svg xmlns="http://www.w3.org/2000/svg"    viewBox="0 0 50 200" preserveAspectRatio="xMidYMid">
        <path 
        vector-effect="non-scaling-stroke"
        d="M48 2 H 2 V198 H48">
    </svg>
</div>


<style define:vars={{strokewidth, strokecolor}}>

/* SVG container */
.svg-container {
    display: flex;       /* Using flexbox */
    justify-content: flex-start; /* Aligns children (SVG) to the right */
    align-items: center; /* Centers children vertically */
    height:  100%;
    
    }

/* SVG styling */
svg {
    overflow: visible;
    width: auto;
    height:  100%;
    display: block; /* Removes inline spacing */
}

/* Keep stroke width constant */
path {
    fill: none;
    stroke: var(--strokecolor);
    stroke-width: var(--strokewidth);
}
</style>