---
interface Props {
 strokewidth?: string;
}

const { strokewidth = "1px" } = Astro.props;
---


<div class="svg-container">
    <svg xmlns="http://www.w3.org/2000/svg"    viewBox="0 0 102 202" preserveAspectRatio="xMidYMid">
        <path 
        vector-effect="non-scaling-stroke"
        d="M2 2 H100 M51 2 V200 M2 200 H100"/>
    </svg>
</div>


<style define:vars={{strokewidth}}>

/* SVG container */
.svg-container {
       width: auto;
       height: auto;
    
    }

/* SVG styling */
svg {
    width: auto;
    height:  auto;
    display: block; /* Removes inline spacing */
}

/* Keep stroke width constant */
path {
    fill: none;
    stroke: #000;
    stroke-width: var(--strokewidth);
}
</style>