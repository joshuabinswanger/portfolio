---
interface Props {
 strokewidth?: string;
 strokecolor?: string;
}

const { strokewidth = "1px", strokecolor = "black" } = Astro.props;
---


<div class:list={["svg-container"]}>
    <svg xmlns="http://www.w3.org/2000/svg"    viewBox="-50 0 100 200" preserveAspectRatio="xMidYMid">
        <path 
        vector-effect="non-scaling-stroke"
        d="M-45 5 H45 M0 5 V195 M-45 195 H45"/>
    </svg>
</div>


<style define:vars={{strokewidth, strokecolor}}>

/* SVG container */
.svg-container {
    display: grid;
    grid-template-columns: 1fr; /* One column spanning the full width */
    grid-template-rows: 1fr;    /* One row spanning the full height */
    justify-content: center;   /* it need both justify content and justify-items to work in firefox and chrome    */
    justify-items: center;       /* Aligns the SVG to the right */
    align-items: center;        /* Centers the SVG vertically */
    width: 100%; 
    }

/* SVG styling */
svg {
    overflow: visible;
    width: auto;
    height:  100%;
    display: block; /* Removes inline spacing */
}

/* Keep stroke width constant */
path {
    fill: none;
    stroke: var(--strokecolor);
    stroke-width: var(--strokewidth);
}
</style>