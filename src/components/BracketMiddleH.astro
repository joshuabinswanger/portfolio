---
interface Props {
 strokewidth?: string;
 strokecolor?: string;
}

const { strokewidth = "1px", strokecolor = "black" } = Astro.props;
---


<div class:list="svg-container">
    <svg xmlns="http://www.w3.org/2000/svg"    viewBox="0 0 102 202" preserveAspectRatio="xMidYMid">
        <path 
        vector-effect="non-scaling-stroke"
        d="M2 2 H100 M51 2 V200 M2 200 H100"/>
    </svg>
</div>


<style define:vars={{strokewidth, strokecolor}}>

/* SVG container */
.svg-container {
       width: auto;
       height: auto;
    
    }

/* SVG styling */
svg {
    width: auto;
    height:  auto;
    display: block; /* Removes inline spacing */
}

/* Keep stroke width constant */
path {
    fill: none;
    stroke: var(--strokecolor);
    stroke-width: var(--strokewidth);
}
</style>