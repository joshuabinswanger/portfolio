---
import type { ImageMetadata } from 'astro';
import { Picture } from 'astro:assets';
import Logo from './Logo.astro';
import SquareBracketV from './SquareBracket_V.astro';
import "../styles/global.css"

const logoPath = "/assets/Rhizome_Test2.riv";

import Index from './Index.astro';

---



<nav id="NavBar" transition:persist>

  <div id="MenuWrapper">
    <div style="pointer-events: auto;">
      <Logo />
    </div>

    <SquareBracketV>
       <Index />
    </SquareBracketV>

    <div id="shift">
    <SquareBracketV>
      <a href="/info" id="MenuItems">
        <p>Info</p>
      </a>
    </SquareBracketV>
   </div>
  
   <div id="shift">
    <SquareBracketV>
      <a href="/contact" id="MenuItems">
        <p>Contact</p>
      </a>
    </SquareBracketV>
  </div>

  <div id="shift">
    <SquareBracketV>
        <a href="/links" id="MenuItems">
          <p>Links</p>
        </a>
   </SquareBracketV>
  </div>
  </div>

</nav>

<style>
@import "../styles/global.css";

#NavBar {
	position: sticky;
  display: grid;
  justify-items: center;
  grid-auto-rows: auto;
  top: var(--padding-desktop);
  height: max-content;

		@media (--bp-mobile) {
			display: none;
		}

    @media (--bp-tablet) {
    top: var(--padding-tablet);
  	}

		@media (--bp-desktop-wide) {
      top: var(--padding-desktop-wide);
		}

}
/* 
#shift {
  margin-top: calc(var(--border-size) * -1.5);
} */

* {
  letter-spacing: 0.8px;
}

#MenuWrapper {
  width: 80%;
  align-items: start;
  justify-items: stretch;
}

a {
  text-decoration: none; /* Removes underline */
  color: inherit; /* Inherits the color from the parent element */
  background: none; /* Removes background if any */
  border: none; /* Removes border if any */
  pointer-events: auto;
} 

p {
  font-size: var(--font-size-dekstop);
  margin: 0px;
  padding: 0.7rem 0.2rem;
  text-align: center;

  @media (--bp-tablet) {
    font-size: var(--font-size-tablet);
  }

  @media (--bp-desktop-wide) {
    font-size: 15px;
  }
}




</style>




