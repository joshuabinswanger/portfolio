---
import ImageElement from "./ImageElement.astro";
import ThumbnailElement from "./ThumbnailElement.astro";
import TopWrapper from "../layouts/TopWrapper.astro";
import GridLayout from "../layouts/GridLayout.astro";
import SquareBracketH from "./SquareBracket_H.astro";
import "../styles/global.css";
import Footer from "./Footer.astro";


---

<TopWrapper>
<!--         <div id="ProfilePic">

        </div> -->

        <p id="AboutText">
    
            Joshua Bisnwanger is a scientific illustrator and designer based in Zurich. He enjoys blending nature’s beauty with technology to create visuals that are both accurate and appealing. Inspired by the natural world, Joshua works on projects ranging from digital art to 3D models, aiming to make complex science more accessible and engaging. With strong communication skills, a diverse technical skill set, and a curious mind, he collaborates with experts to produce work that sparks curiosity and highlights the wonders of our world.        
    
        </p>
</TopWrapper>


<style>
@import "../styles/global.css";

#AboutText {
    grid-column-start: 2;
    font-size: var(--font-size-dekstop);
    max-width: 111ch;
    justify-self: center;
    letter-spacing: 2px;
    font-family: PowerGrotesk-Light;
    font-weight: 200;
    
    margin-top: calc(var(--padding-desktop) + 48px + 0.2rem);
    

    @media (--bp-mobile) {
        font-size: var(--font-size-mobile);
        padding: 0 7px;
        margin-top: var(--padding-mobile);
    }

    @media (--bp-tablet) {
        grid-column-start: 2;
        font-size: var(--font-size-tablet);
        margin-top: calc(var(--padding-tablet) + 36px + 0.2rem);
    }

    @media (--bp-desktop-wide) {
        grid-column-start: 2;

        font-size: var(--font-size-desktop-wide);
        margin-top: calc(var(--padding-desktop-wide) + 48px + 0.2rem);
    }
}

</style>